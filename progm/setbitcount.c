#include<stdio.h>
int main()
{
	:


}


/*{
	int data,count,bit;
        count=0,bit=31;
        scanf("%d",&data);
        do{
                if((data>>bit)&1){
                        count++;
                }
                bit--;
        }while(bit>=0);
        printf("data %d has %d set bits\n",data,count);
}



{
	int data,count,bit;
	count=0,bit=31;
	scanf("%d",&data);
	while(bit>=0){
		if((data>>bit)&1){
			count++;
		}
		bit--;
	}
	printf("data %d has %d set bits\n",data,count);
}
*/
