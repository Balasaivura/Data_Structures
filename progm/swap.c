#include<stdio.h>
#include<ctype.h>
int main()
// Changing upper to lower or lower to upper using if and bitwise XOR operator
/*{
	char ch;
input:printf("Enter an Alphabet:\n");
      ch=getchar();
      if(!isalpha(ch)){
              goto input;
      }
      ch^=32;
      printf("ch=%c\n",ch);
}


// Changing upper to lower or lower to upper using if else
{
	char ch;
input:printf("Enter an Alphabet:\n");
      ch=getchar();
      if(!isalpha(ch)){
	      goto input;
      }
      if(isupper(ch)){
	      ch+=32;
      }
      else{
	      ch-=32;
      }
      printf("ch=%c\n",ch);
}


// Nibbles of Char Swaped
{
	//char ch='4';
	char ch;
	printf("Enter a character(a,A,0,@):\n");
	scanf("%c",&ch);
	printf("ch=%d %c\n",ch,ch);
	ch=(ch<<4)|(ch>>4);
	printf("ch=%d %c\n",ch,ch);
}


//bytes of a short integer swaped
{
	//unsigned short int v=0x1234;
	unsigned short int v;
	printf("Enter a hex number(0x1234):\n");
	scanf("%hx",&v);
	printf("%hx\n",v);
	v=(v<<8)|(v>>8);
	printf("%hx\n",v);
}
*/
