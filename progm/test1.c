#include<stdio.h>
int main()




	
/*{
	int d,m,y;
	printf("enter the values in these format dd mm yyyy :");
	scanf("%d%*c%d%*c%d",&d,&m,&y);
	printf("%d %d %d\n",d,m,y);
	return 0;
}



int main()
{
	float x,y;
	scanf("%f %f",&x,&y);
	printf("x=%4.1f,y=%7.5f",x,y);//here they will print x as 4 digits including decimal & y as 7 digits including decimal
	return 0;
}

int main()
{
	float x,y;
	scanf("%3f %4f",&x,&y);//the value before the format specifier having the total width of data including the digits before and after decimal and including decimal itself
	printf("%3f %4f",x,y);
	return 0;
}
*/
